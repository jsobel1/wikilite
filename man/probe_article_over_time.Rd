% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/article_history.R
\name{probe_article_over_time}
\alias{probe_article_over_time}
\title{Probe a Wikipedia article at multiple time points}
\usage{
probe_article_over_time(
  article_name,
  dates_to_probe,
  lang = "en",
  metrics = c("sci_score", "doi_count", "ref_count", "size")
)
}
\arguments{
\item{article_name}{Character string — Wikipedia article title.}

\item{dates_to_probe}{Character vector of ISO 8601 timestamps
(e.g. \code{"2021-01-01T00:00:00Z"}).}

\item{lang}{Two-letter language code (default: \code{"en"}).}

\item{metrics}{Character vector of metrics to compute.  Any subset of
\code{c("sci_score", "doi_count", "ref_count", "size")}.}
}
\value{
A data frame with one row per successfully fetched date and columns
  \code{date}, \code{art}, and one column per requested metric.
}
\description{
For each timestamp in \code{dates_to_probe}, fetches the article snapshot
and computes the requested quality metrics.  This implements the monthly
probing approach used in COVID-19 citation analysis.
}
\examples{
\donttest{
dates <- paste0(2018:2023, "-01-01T00:00:00Z")
probe_article_over_time("Zeitgeber", dates)
}
}
